# Action-optimization model snapshot

These files preserve action-optimization branch variants that differed from
the primary model store when `stretch_mujoco/models` became shared. They are
historical references only. The authoritative runtime files are in the primary
worktree's `stretch_mujoco/models` directory.
